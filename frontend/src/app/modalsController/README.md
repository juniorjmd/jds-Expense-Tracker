# Modals Controller

All reusable Angular modal components live here.

New modal UI must be created in this folder as a standalone component and then imported by the page that opens it. Page components should keep orchestration/state, while modal components own the dialog shell, layout, and controls.
